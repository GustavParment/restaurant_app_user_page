import React from 'react'
import { IUser } from '../types/IUser'

const SwipeCard = (user: IUser) => {
    const getMatchingHobbies = () =>{
        if(user.profile)
    }
  return (
    <div>
      
    </div>
  )
}

export default SwipeCard
